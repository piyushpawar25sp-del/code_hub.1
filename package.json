{
  "name": "codehub-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npx http-server dist -p 8080",
    "serve": "npm run build && npm run start"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
