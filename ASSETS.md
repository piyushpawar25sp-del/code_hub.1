Place the provided images into the `assets/` folder with these filenames:

- `assets/codehub-logo.png`  — main navbar logo. Use the exact image you provided (recommended size: ~40×40, or SVG/PNG).
- `assets/logo-hero.png`     — (optional) larger logo for the hero (approx 120×120)
- `assets/hero-banner.jpg`   — (optional) hero banner image (approx 1280×480 or cropped)

If you upload a PNG/SVG with a different name, update the `src` paths in the navbar or hero accordingly. The project currently expects `assets/codehub-logo.png` for the navbar.

Create the `assets/` folder next to `index.html` and add the images there. After uploading, I can replace any references and fine-tune sizing.
